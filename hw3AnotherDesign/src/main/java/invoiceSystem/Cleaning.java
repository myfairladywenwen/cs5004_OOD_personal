package invoiceSystem;

public class Cleaning extends Interior {

  public Cleaning(String address, Size size, boolean monthly, int preService, int numPets) {
    super(address, size, monthly, preService, numPets);
  }
}
