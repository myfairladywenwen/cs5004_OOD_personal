package invoiceSystem;

public class Exterior extends AbRegular {

  public Exterior(String address, Size size, boolean monthly, int preService) {
    super(address, size, monthly, preService);
  }
}
